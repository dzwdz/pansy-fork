ls - list files in a directory

USAGE
$ ls [ directory ... ]

DESCRIPTION
If ls is given no arguments, ls prints all files in the current working
directory. If one or more directories are provided as arguments, ls prints the
files in those directories instead.