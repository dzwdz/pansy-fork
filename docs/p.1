p - paginate

USAGE
$ p [ -number ] [ file ... ]

DESCRIPTION
p copies from its standard input, or the named files if given, to the its
standard output. It stops at every 22nd line and waits for the user to input a
newline. The option sets the number of lines between each stop, changing the
default of 22 lines per page.
