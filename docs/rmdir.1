rmdir - remove empty directories

USAGE
$ rmdir [ directory ... ]

DESCRIPTION
rmdir removes all directories provided as arguments. The program will produce
errors if the callee does not have sufficient permissions to remove the given
directory or if the directory in question is non-empty.

SEE ALSO
rm.1
